const person = ['john doe', 'male', 23];
const friends = ['marzio', 'lisa', 'steffen'];
const favoriteFood = ['avocado', 'nuts', 'fruits'];

person.map((person) => console.log(person));
friends.map((friend) => console.log(friend));
favoriteFood.map((food) => console.log(food));
